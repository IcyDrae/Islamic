name: Java Continous Integration Pipeline
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: Java/Islamic
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven
      - name: Build project
        run: mvn clean package
      # Later when there will be tests
      #- name: Run tests
      #  run: mvn test
      - name: Upload JAR
        uses: actions/upload-artifact@v4
        with:
          name: Islamic-JAR
          path: Java/Islamic/target/*.jar
